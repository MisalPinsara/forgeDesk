# Use /api locally with the Vite proxy, or set this to the deployed server API URL.
VITE_API_BASE_URL="/api"
VITE_SERVER_ORIGIN="http://localhost:3000"
# Leave empty in development to use the Vite proxy. Set this only when the
# deployed authentication service has a different public origin.
VITE_AUTH_BASE_URL=""
